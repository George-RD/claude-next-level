Study every test file listed in the manifest using separate subagents per file and document each in specs/tests/{basename}_spec.md.

Link the implementation as citations in the specification using [test:file:line-range] format.

Every behavioral claim must cite the source with exact line numbers. Describe WHAT is being tested, not HOW.
